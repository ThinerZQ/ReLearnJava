package com.zq.virtualmechine.classload;

import com.sun.jmx.snmp.tasks.Task;

import java.util.LinkedList;
import java.util.Queue;

/**
 * Created with IntelliJ IDEA
 * Date: 2016/3/5
 * Time: 11:29
 * User: ThinerZQ
 * GitHub: <a>https://github.com/ThinerZQ</a>
 * Blog: <a>http://www.thinerzq.me</a>
 * Email: 601097836@qq.com
 */
public class Tr {
    public static void main(String[] args) {

    }
    public void stateMachine(Task task){
      /*  Queue queue = new LinkedList();
        int count=0;
        int JudgeCount=3;
        int DecomposeCount=2;
        int DecomposeVoteCount=3;
        int SolveCount=2;
        int SolveVoteCount=3;
        while (true){
            if (queue.poll() =="judgeComplete")
                count++;
            if (count == JudgeCount) {
                count=0;
                goto
            }
        }
        while (true){
            if (queue.poll() =="decomposeComplete"){
                add decompose result to set
                count++;
            }
            if (count == DecomposeCount) {
                count=0;
                goto
            }
        }
        while (true){
            if (queue.poll() =="decomposeVoteComplete")
                count++;
            if (count == DecomposeVoteCount) {
                get the best decompose result
                step =best decompose result size
                for (int i = 0; i < steps; i++) {
                     stateMachine(decompose result[i])
                }
                count=0;
                goto
            }
        }
        while (true){
            if (queue.poll() =="finish")
                count++;
            if (count == step) {
                count=0;
                goto
            }
        }
        while (true){

                goto
            }
        }
        while (true){
            if (queue.poll() =="decomposeComplete")
                count++;
            if (count == DecomposeCount) {
                count=0;
                goto
            }
        }*/
    }
}
